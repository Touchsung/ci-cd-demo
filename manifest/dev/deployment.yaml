apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-node-app-touch-deployment-dev
  namespace: group-4
  labels:
    app: simple-node-app-touch-deployment-dev
spec:
  replicas: 1
  revisionHistoryLimit: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 50%
  selector:
    matchLabels:
      app: simple-node-app-touch-deployment-dev
  template:
    metadata:
      labels:
        app: simple-node-app-touch-deployment-dev
    spec:
      containers:
        - name: simple-node-app-touch-dev
          image: touchsung/simple-node-app:f383e0a29c0665381c45707fae5acd50f18a5b8b
          ports:
            - containerPort: 3000
